import React from 'react';

const SettingsNavList = ({ settingsClick }) => {
  return (
    <div>
      <button onClick={settingsClick}>Settings</button>
    </div>
  )
};

export default SettingsNavList;
